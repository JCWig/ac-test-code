<section class="util-hide">
  {{#each subMenuItems}}
    <div>
      <a href="{{url}}"><i class="context-{{contextId}}"></i>{{name}}</a>
      {{#if subMenuItems}}<i class="arrow-down"></i>{{/if}}
    </div>
    {{#if subMenuItems}}
    {{! recursive partial just in case the context selector ever has more than 2 levels of nesting }}
      {{> contextSelectorSection }}
    {{/if}}
  {{/each}}
</section>
